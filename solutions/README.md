# Solution Index

Each problem lives in its own directory with:

- `solution.cpp` — the reviewed C++ solution;
- `README.md` — summary, approach, correctness, complexity, edge cases, and learning notes.

Problems are added in learning order, with one focused Git commit per problem.

